[tool.poetry]
name = "Python3 main.py"
version = "0.1.0"
description = ""
authors = ["techlolalexy@gmail.com"]

[tool.poetry.dependencies]
python = "^3.8"
Flask = "^2.0.1"
tasks = "^2.8.0"
google-api-python-client = "^2.18.0"
discord = "^1.7.3"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
